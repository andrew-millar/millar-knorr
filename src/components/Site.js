import React from "react";

const Site = props => (
    {Object.keys(this.state.pages).map(page=>(
        <div className="site">
            <figure></figure>
            <div className="desc">
                <p>
                    props.decs
                </p>
            </div>
        </div>
    ))}; 
);

export default Site;