import React from 'react';

const Section = props => (
    <main>
        {Object.keys(this.state.sectionClasses).map(sectionClass => (
            <section className={this.props.}>
                
            </section>
        ))};
    </main>
)

export default Section;